/* array functions
    - forEach // simple for loop
    - map // loop but it returns an new array with modified values
    - filter // loop but it returns new array by filtering the existing array elements
    - find // loop but it returns the single element which satisfy our condition 
*/